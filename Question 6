function DashrunAnimation
  if player = DashrunSkill than
addEvent(DashrunSkillAnimation, 1000)
  end
    return true
end
function onCastSkill(player,self)
  if player than 
  onCastSkill = get5Blocks ahead
  end
    end
    return true
end

On video 6 the player is dashing so i coded as a skill not a spell since is self inflicted. And put it as 5 block from his direction.
